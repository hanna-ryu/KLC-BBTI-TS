import React from 'react';
import styled from 'styled-components';

const CreateGlobalStyle = styled.div`
  height: calc(var(--vh, 1vh) * 100);
  width: 100vw;
  background-color: #ffffff;
`;

export { CreateGlobalStyle };
